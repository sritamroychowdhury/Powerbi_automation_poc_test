pytest>=8.0
pytest-bdd>=7.0
snowflake-connector-python>=3.6
cryptography>=41.0
playwright>=1.45
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
msal>=1.28
boto3>=1.34
# allure-pytest-bdd only -- NOT allure-pytest too, they both register --alluredir and conflict.
# See README.md "Test reporting with Allure" for why.
allure-pytest-bdd>=2.13
